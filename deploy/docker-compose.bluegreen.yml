# Overlay blue-green per insight-magellano
# Uso: docker compose -f deploy/docker-compose.prod.yml -f deploy/docker-compose.bluegreen.yml --profile green up -d
# Switch: deploy/scripts/switch-upstream.sh blue|green

services:
  nginx:
    depends_on:
      - backend-blue
      - backend-green
